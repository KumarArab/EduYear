import 'package:flutter/material.dart';

class Banners extends StatelessWidget {
  static const routeName = "/banners";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Banners to be shown here"),
      ),
    );
  }
}
